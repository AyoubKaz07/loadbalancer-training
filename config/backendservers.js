// Map to store the health status of each backend server (true for healthy, false for unhealthy)
const backendservers = [
    'http://localhost:8081',
    'http://localhost:8082',
    'http://localhost:8083',
]

module.exports = backendservers;